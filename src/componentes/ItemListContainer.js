const ItemListContainer = (props) => {
    return (
        <section id="productos">
            <h2>{props.greeting}</h2>
            <p>Acá estará el catálogo</p>
        </section>
    );
}

export default ItemListContainer;